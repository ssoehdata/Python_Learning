#indexing within strings
# index values must be an integer and starts at zero
# '[]' are used for indexes
fruit = 'banana'
print(fruit)
letter = fruit[1]
print(letter)

# index value can be an expression that is computed
x = 3
w = fruit[x-1]
print(w)

zot = 'abc'
print(zot[2])

# len() function on a string: (0 through length - 1)
# fruit is a string variable here:
# pass fruit into the len variable as a parameter:
fruit = 'banana'
print(len(fruit))

fruit = 'banana'
x = len(fruit)
print(x)

# function is a some stored code that takes some input
# and produces an ouput.